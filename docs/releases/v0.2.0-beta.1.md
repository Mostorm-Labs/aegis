# Aegis v0.2.0-beta.1

Status: **Prerelease — Control Plane v0.2 / Plugin + exact nine Skills**

## What this release is

- first public prerelease candidate of the Aegis Control Plane v0.2 product line;
- one native Aegis Plugin exposing exactly nine canonical Skills;
- durable Control State / Authority / Gate / resume semantics through the Plugin and governed execution surfaces;
- accepted Control Plane exact candidate `18559f32ede7ebd845064fe8de7967ca358b785f`;
- Control Plane P34 review `5108520468 = PASS / ACCEPTED_FOR_DOWNSTREAM`;
- Current Control Plane Verification Authority `18b374e95057bafd0feac3ca16e7aca4774a925a`;
- repository-backed execution includes the separately Gate-accepted and integrated repository-identity preflight repair.

This file describes the candidate identity. It does **not** claim that `v0.2.0-beta.1` has already been published.

## Key v0.2 capabilities

- explicit Trusted Basis, Control Cursor, and Execution Cursor separation;
- sessionless/interrupted-work resume with fail-closed divergence handling;
- verification-bound implementation packages and reviewer-resolvable durable materialization boundaries;
- bounded repair/reverification lineage;
- independent P34 Gate ownership;
- explicit repository identity before package/anchor/cursor reconciliation for repository-backed execution;
- exact-nine Plugin distribution with coherent cross-Skill and cross-surface handoffs.

## Deliberate boundaries

- no standalone daemon, agent, or hosted Control Service is required;
- no R0/S0/W7D service-scale claim is made;
- `SERVICE_PROFILE` is not authorized;
- rollout remains denied beyond the accepted Plugin profile;
- zero-user-turn cross-Primary substantive chaining is not claimed or authorized;
- historical PP0 40-WorkScope, seven-oracle, and 32-mutant obligations are not v0.2 release requirements under Current Verification Authority.

## Distribution

The intended prerelease distribution is:

- GitHub-backed native Aegis Plugin;
- `aegis-skill-installation-kit-v0.2.0-beta.1.zip` fallback containing exactly nine nested Skill ZIPs;
- `aegis-release-v0.2.0-beta.1.json` release manifest;
- `SHA256SUMS` generated by the release workflow.

Publication remains owned by `.github/workflows/release.yml` after final P24 release-readiness approval.

## Upgrade / rollback

- normal update is a whole-catalog exact-nine Plugin transition;
- partial or mixed Aegis catalogs fail closed;
- immutable `v0.1.0-beta.3` remains the previous published rollback and reproducibility boundary;
- the `v0.1.0-beta.3` published source, release manifest, Plugin payload, tag, Release, and assets remain historical evidence and are not rewritten by this candidate.

## Verification

Final P24 release-readiness rereview is required before publication. The exact candidate must provide reviewer-resolvable evidence for:

- deterministic `0.2.0-beta.1` release manifest coherence;
- exact-nine native Plugin materialization and canonical Skill parity;
- current repository-identity candidate parity;
- immutable beta.3 historical source binding;
- deterministic Installation Kit construction and nested ZIP digest verification;
- Skillset, Project State, Control Plane, corpus, and evaluation regressions;
- absence of tag/Release collision before publication.

The release workflow must rerun the required verification on the exact authorized release source before creating public release assets.
