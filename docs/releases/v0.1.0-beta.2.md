# Aegis v0.1.0-beta.2

Status: **Prerelease — 9-Skill Installation Kit V1 + accepted native Plugin distribution**

This prerelease refreshes the Aegis nine-Skill Installation Kit on the reconciled Project State v0.5 baseline after Execution Anchor + Resume Cursor v0.2 was integrated into `main`. The same coherent `0.1.0-beta.2` source set has also completed the independent Plugin Distribution P34 product Gate.

## Included

- one `aegis-skill-installation-kit-v0.1.0-beta.2.zip` Release Asset;
- `release.json` with exact nine Skill identities, source-tree SHA-256 digests, upload ZIP filenames, and upload ZIP SHA-256 digests;
- nine directly uploadable Skill ZIPs:
  - `aegis.zip`
  - `aegis-project-state.zip`
  - `aegis-discovery.zip`
  - `aegis-modeling.zip`
  - `aegis-architecture.zip`
  - `aegis-verification.zip`
  - `aegis-governance.zip`
  - `aegis-implementation.zip`
  - `aegis-gate-review.zip`
- `aegis-release-v0.1.0-beta.2.json` as a standalone release manifest;
- `SHA256SUMS` for the distributable assets.

The native Plugin remains a repository/marketplace distribution adapter rather than an additional GitHub Release archive: one Aegis Plugin materializes the same exact-nine canonical Skill catalog.

## Changes since v0.1.0-beta.1

- incorporates Execution Anchor + Resume Cursor v0.2 into the canonical Aegis execution surface;
- preserves the invariant `Task Anchor != Execution Cursor`;
- baseline-dependent repository packages require a non-null `task_anchor`;
- once P33 has accepted a continuation point, the handoff requires a non-null `resume_cursor`;
- resumable work distinguishes exact cursor, descendant cursor, anchor-descendant without cursor, and genuine divergence rather than using historical HEAD equality as the sole predicate;
- accepted completed work is not replayed during descendant reconciliation, while genuine divergence fails closed;
- incorporates the immutable PR #16 Gate Decision and `int-pr16` repository integration occurrence into Project State v0.5;
- refreshes the nine Skill release identities and native Plugin materialization from the reconciled canonical Skill trees;
- completes Plugin Distribution P34 evidence for one-install exact-nine provenance, protected behavioral parity, coherent whole-catalog upgrade, and invalid-upgrade fail-closed safety.

## Verification inherited by this catalog

The release workflow reruns Project State, Skillset, distribution, routing, installed-platform, repository regression, and evaluation checks on the exact source revision before publishing. It then deterministically builds the Installation Kit, verifies nested Skill ZIP digests and top-level asset checksums, preserves the P24 build evidence artifact, and only then creates the immutable GitHub prerelease.

The Plugin Distribution P34 product Gate is independently accepted. Its five required subcases all passed: source materialization, one real Plugin install to exact nine, protected behavioral parity, valid in-place whole-catalog upgrade, and invalid-upgrade fail-closed behavior. The final aggregate review is PR #13 review `5059882131`.

## Installation Kit

1. Download `aegis-skill-installation-kit-v0.1.0-beta.2.zip` from this Release.
2. Verify it against `SHA256SUMS` if desired.
3. Extract the outer archive once.
4. Upload the nine nested Skill ZIPs to ChatGPT Skills without unpacking those nested ZIPs.
5. Treat any partial Aegis-family catalog as incomplete; normal Multi-Skill use requires the exact nine-Skill catalog.

## Lifecycle boundary

Plugin Distribution P34 acceptance and P24 publication do not by themselves rewrite Project State lifecycle facts. At publication time `aegis-plugin-distribution-v0.1` remains `Proposed`; Authority promotion, repository integration, and the durable Project State integration occurrence are completed separately in the PR #13 finishing/integration closure.

This prerelease also does not promise stability for future platform marketplace APIs beyond the verified `0.1.0-beta.2` behavior.

## Provenance

The release tag pins the exact source revision that passes the P24 release workflow. The GitHub Release body records that source revision and the release workflow run that produced and verified the assets.
