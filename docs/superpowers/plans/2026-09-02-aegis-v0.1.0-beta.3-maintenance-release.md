# Aegis v0.1.0-beta.3 Maintenance Release Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Publish `v0.1.0-beta.3` from the current released `main` line with the exact Gate-accepted PR #45 Codex handoff-prefix semantics, a coherent Plugin marketplace version bump, and a refreshed nine-Skill Installation Kit, without importing the unreleased Control Plane v0.2 stack.

**Architecture:** Treat PR #45 (`af96b57b2f33b6ce629ca6e39644d6e96934c304`) as the semantic source and current `main` (`a77fca147ead0d66484201cad30d8c6a80d11f8e`) as the maintenance-release base. Transplant only PR #45's accepted handoff/Skill/test delta, regenerate release identity as `0.1.0-beta.3`, update the repository marketplace and Plugin manifests, validate on a maintenance PR, then merge, tag, publish, and verify immutable release artifacts. The unreleased Control Plane v0.2 ancestry is explicitly excluded; v0.2 inherits the maintenance delta later at an authorized integration boundary.

**Tech Stack:** Git/GitHub, JSON manifests, Markdown Skill contracts, Python/pytest repository validation, GitHub Actions release workflow.

**Spec:** PR #45 exact head `af96b57b2f33b6ce629ca6e39644d6e96934c304`; P34 review `5086435221` (`PASS / ACCEPTED_FOR_DOWNSTREAM`).

## Global Constraints

- Maintenance base is exact `main` head `a77fca147ead0d66484201cad30d8c6a80d11f8e` unless a fresh-state check proves a legal descendant before merge.
- PR #45 semantic source is exact head `af96b57b2f33b6ce629ca6e39644d6e96934c304`.
- Do not retarget or merge PR #45 directly to `main`; its ancestry contains the unreleased Control Plane v0.2 stack.
- New release version is exactly `0.1.0-beta.3`; immutable `v0.1.0-beta.2` tag, release, manifest, and assets are not rewritten.
- Exact Codex execution prefix remains: `请按以下 Aegis handoff 直接执行：以 \`package_ref\` 为任务授权，按 \`task_anchor/resume_cursor\` 核对当前状态并从首个未完成步骤继续；若状态冲突则 fail closed。`
- The prefix remains rendering/execution-trigger metadata only; it does not expand Current Authority, package scope, stage ownership, Gate authority, or Project State.
- Plugin marketplace version, Plugin manifest version, release manifest filename/version, docs, tag, and release asset version must agree on `0.1.0-beta.3`.
- The release workflow must publish new immutable artifacts; it must not replace the beta.2 release.
- v0.2 CP-I09 P32 evidence execution remains isolated and must not be rebased or mutated as part of this maintenance release.

---

### Task 1: Materialize the Gate-accepted PR #45 semantic delta on the maintenance line

**Files:**
- Modify: `docs/execution-surface-contract-v0.2.md`
- Modify generated/canonical `handoff-contract.md` copies under `skillset/shared`, `skills/*/references/shared`, and `plugins/aegis/skills/*/references/shared`
- Modify: `skillset/skills/aegis-implementation/SKILL.md`
- Modify: `skills/aegis-implementation/SKILL.md`
- Modify: `plugins/aegis/skills/aegis-implementation/SKILL.md`
- Modify: `skillset/dogfood/pd-p34-04-valid-upgrade-v0.1.json`
- Create: `tests/skillset/test_codex_handoff_execution_prefix.py`
- Modify the PR #45 regression files: `tests/skillset/test_distribution_contract.py`, `tests/skillset/test_pd_p34_04_plugin_valid_upgrade.py`, `tests/skillset/test_plugin_package.py`

**Interfaces:**
- Consumes: exact PR #45 materialized head `af96b57b2f33b6ce629ca6e39644d6e96934c304`.
- Produces: a maintenance-line tree whose PR #45-owned files are semantically identical to the accepted head, except release-version identity is handled separately in Task 2.

- [ ] **Step 1: Transplant exact accepted blobs**

Use the blobs from PR #45 exact head for every PR #45 semantic/test path listed above. Do not copy the PR #45 modified `skillset/releases/aegis-0.1.0-beta.2.json` into the maintenance branch.

- [ ] **Step 2: Verify the maintenance diff contains no Control Plane v0.2 implementation files**

Compare the maintenance branch to `a77fca147ead0d66484201cad30d8c6a80d11f8e`; expected changed paths are limited to PR #45 semantic/test files plus the beta.3 release/version/docs files defined in Task 2.

- [ ] **Step 3: Commit the semantic transplant**

Commit message: `feat: bring Codex handoff prefix to v0.1 release line`.

### Task 2: Materialize coherent `0.1.0-beta.3` distribution identity

**Files:**
- Modify: `.agents/plugins/marketplace.json`
- Modify: `plugins/aegis/.codex-plugin/plugin.json`
- Create: `skillset/releases/aegis-0.1.0-beta.3.json`
- Modify: `README.md`
- Modify: `docs/installation-and-usage-v0.1.md`
- Create: `docs/releases/v0.1.0-beta.3.md`

**Interfaces:**
- Consumes: Task 1 canonical Skill trees.
- Produces: one coherent repository/catalog/release identity `0.1.0-beta.3` suitable for Plugin sync and GitHub Release packaging.

- [ ] **Step 1: Bump Plugin and marketplace versions**

Set `.agents/plugins/marketplace.json` plugin `version` to `0.1.0-beta.3` and `plugins/aegis/.codex-plugin/plugin.json` `version` to `0.1.0-beta.3`; preserve all other manifest semantics.

- [ ] **Step 2: Generate the beta.3 release manifest**

Run `python3 scripts/build_aegis_distributions.py --version 0.1.0-beta.3 --write-manifest`, then require `python3 scripts/build_aegis_distributions.py --version 0.1.0-beta.3 --check` to print `AEGIS_DISTRIBUTION_STATE_OK`.

- [ ] **Step 3: Update user-facing release references**

Update current-version installation references in `README.md` and `docs/installation-and-usage-v0.1.md` from beta.2 to beta.3. Do not invent a beta.3 Installation Kit checksum before release publication; where the guide currently embeds the beta.2 immutable checksum, replace the current-version section with verification through the release-provided `SHA256SUMS`/release manifest, while preserving beta.2 historical documentation only where explicitly historical.

- [ ] **Step 4: Add beta.3 release notes**

Document that beta.3 is a maintenance prerelease carrying the PR #45 Codex handoff-prefix semantics, refreshes Plugin/catalog identity and the nine-Skill Installation Kit, preserves beta.2 immutability, and intentionally excludes unreleased Control Plane v0.2 implementation work.

- [ ] **Step 5: Commit the release identity update**

Commit message: `release: prepare Aegis v0.1.0-beta.3`.

### Task 3: Hosted verification and maintenance Gate

**Files:**
- Test only; no production changes unless a verified defect requires P35/P36 repair.

**Interfaces:**
- Consumes: exact maintenance PR head.
- Produces: reviewer-accessible CI evidence and a Gate/governance decision for merge/release readiness.

- [ ] **Step 1: Open a PR to `main`**

Title: `release: Aegis v0.1.0-beta.3 maintenance update`.

- [ ] **Step 2: Require exact-head hosted checks**

Require Aegis Skillset Integrity and Project State Integrity to pass. Validate generated distributions, OpenAI Plugin materialization, routing, installed-platform checks, release manifests, Skillset regressions, Project State regressions, and the protected evaluation corpus.

- [ ] **Step 3: Independently confirm release identity coherence**

Confirm exact head has marketplace `0.1.0-beta.3`, Plugin manifest `0.1.0-beta.3`, `skillset/releases/aegis-0.1.0-beta.3.json`, PR #45 prefix regressions, and no imported Control Plane v0.2 implementation paths.

- [ ] **Step 4: Run maintenance Gate/release-readiness review**

Fail closed on semantic drift, mixed release identity, regression failure, or unexpected v0.2 scope. Merge is authorized only after the maintenance result is accepted for integration/release.

### Task 4: Merge, publish, and verify immutable beta.3

**Files:**
- Git refs / GitHub Release assets; no post-tag source mutation.

**Interfaces:**
- Consumes: accepted exact maintenance PR head.
- Produces: merged `main`, immutable tag `v0.1.0-beta.3`, GitHub prerelease, refreshed Plugin marketplace state, and verified nine-Skill Installation Kit assets.

- [ ] **Step 1: Merge fail-closed against the accepted head**

Use the exact reviewed head SHA when merging the maintenance PR.

- [ ] **Step 2: Verify post-merge `main` and CI**

Resolve the merge result from `main` and require post-merge integrity checks to remain green before tagging.

- [ ] **Step 3: Create immutable tag `v0.1.0-beta.3`**

Tag the exact accepted release commit. Do not move or replace existing release tags.

- [ ] **Step 4: Let `.github/workflows/release.yml` publish**

Require the release workflow to validate tag/manifest/plugin version coherence, build the deterministic nine-Skill Installation Kit, publish `aegis-skill-installation-kit-v0.1.0-beta.3.zip`, publish the standalone release manifest and `SHA256SUMS`, and verify the uploaded asset digest.

- [ ] **Step 5: Verify the published release independently**

Confirm release target equals tag target, version identities equal `0.1.0-beta.3`, required assets exist, and published digests match the workflow evidence.

- [ ] **Step 6: Preserve a forward-integration obligation for v0.2**

Record that the beta.3 maintenance delta must be reconciled into the v0.2 integration baseline after CP-I09 evidence closure; do not mutate the active CP-I09 branch solely to consume beta.3.
