# Aegis v0.1.0-beta.3 Maintenance Release Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Publish `v0.1.0-beta.3` from the current released `main` line with the exact Gate-accepted PR #45 Codex handoff-prefix semantics, a coherent Plugin manifest version update behind the existing repository marketplace source, and a refreshed nine-Skill Installation Kit, without importing the unreleased Control Plane v0.2 stack.

**Architecture:** Treat PR #45 (`af96b57b2f33b6ce629ca6e39644d6e96934c304`) as the semantic source and current `main` (`a77fca147ead0d66484201cad30d8c6a80d11f8e`) as the maintenance-release base. Transplant only PR #45's accepted handoff/Skill/test delta, regenerate release identity as `0.1.0-beta.3`, keep `.agents/plugins/marketplace.json` source/path stable, advance the native Plugin manifest version, validate on a maintenance PR, then merge, tag, publish, and verify immutable release artifacts. The unreleased Control Plane v0.2 ancestry is explicitly excluded; v0.2 inherits the maintenance delta later at an authorized integration boundary.

**Tech Stack:** Git/GitHub, JSON manifests, Markdown Skill contracts, Python/unittest repository validation, GitHub Actions release workflow.

**Spec:** PR #45 exact head `af96b57b2f33b6ce629ca6e39644d6e96934c304`; P34 review `5086435221` (`PASS / ACCEPTED_FOR_DOWNSTREAM`).

## Global Constraints

- Maintenance base is exact `main` head `a77fca147ead0d66484201cad30d8c6a80d11f8e` unless a fresh-state check proves a legal descendant before merge.
- PR #45 semantic source is exact head `af96b57b2f33b6ce629ca6e39644d6e96934c304`.
- Do not retarget or merge PR #45 directly to `main`; its ancestry contains the unreleased Control Plane v0.2 stack.
- New release version is exactly `0.1.0-beta.3`; immutable `v0.1.0-beta.2` tag, release, manifest, and assets are not rewritten.
- Exact Codex execution prefix remains: `请按以下 Aegis handoff 直接执行：以 \`package_ref\` 为任务授权，按 \`task_anchor/resume_cursor\` 核对当前状态并从首个未完成步骤继续；若状态冲突则 fail closed。`
- The prefix remains rendering/execution-trigger metadata only; it does not expand Current Authority, package scope, stage ownership, Gate authority, or Project State.
- `.agents/plugins/marketplace.json` has no release-version field and remains a stable source/path declaration. Version identity is owned by `plugins/aegis/.codex-plugin/plugin.json` plus the release manifest/tag/assets.
- Plugin manifest version, release manifest filename/version, candidate CI, docs, tag, and release asset version must agree on `0.1.0-beta.3`.
- The mutable current generated-distribution oracle `skillset/releases/aegis-0.1.0-task6.1.json` must track the current canonical Skill trees; the published beta.2 manifest remains historical and unchanged.
- The release workflow must publish new immutable artifacts; it must not replace the beta.2 release.
- v0.2 CP-I09 P32 evidence execution remains isolated and must not be rebased or mutated as part of this maintenance release.

---

### Task 1: Materialize the Gate-accepted PR #45 semantic delta on the maintenance line

**Files:**
- Modify: `docs/execution-surface-contract-v0.2.md`
- Modify canonical/generated `handoff-contract.md` copies under `skillset/shared`, `skills/*/references/shared`, and `plugins/aegis/skills/*/references/shared`
- Modify canonical/generated `aegis-implementation/SKILL.md` copies
- Modify canonical/generated `aegis-implementation/references/implementation-control.md` copies
- Modify: `tests/skillset/test_execution_surface_contract.py`
- Modify: `tests/skillset/test_pd_p34_04_plugin_valid_upgrade.py`
- Refresh mutable current generated-distribution oracle: `skillset/releases/aegis-0.1.0-task6.1.json`

**Interfaces:**
- Consumes: exact PR #45 materialized head `af96b57b2f33b6ce629ca6e39644d6e96934c304`.
- Produces: a maintenance-line tree whose PR #45-owned semantic/test files match the accepted head while historical beta.2 release identity remains immutable.

- [x] **Step 1: Transplant exact accepted semantic/test blobs**

The exact PR #45 net semantic/test delta was projected onto a branch created directly from current `main`. The PR #45 modified historical `skillset/releases/aegis-0.1.0-beta.2.json` was intentionally not copied.

- [x] **Step 2: Refresh the mutable current distribution oracle**

Hosted RED proved that `scripts/build_aegis_distributions.py --check` binds the current canonical Skill trees to `aegis-0.1.0-task6.1.json`. The PR #45 accepted `task6.1` manifest was therefore applied while beta.2 remained unchanged.

- [x] **Step 3: Verify maintenance isolation**

`main...maintenance-head` contains no `tools/aegis_control`, CP-Ixx workflow, or Control Plane v0.2 implementation path.

### Task 2: Materialize coherent `0.1.0-beta.3` distribution identity

**Files:**
- Keep unchanged: `.agents/plugins/marketplace.json`
- Modify: `plugins/aegis/.codex-plugin/plugin.json`
- Create: `skillset/releases/aegis-0.1.0-beta.3.json`
- Modify: `.github/workflows/skillset.yml`
- Modify: `tests/skillset/test_openai_plugin_materialization.py`
- Modify: `README.md`
- Modify: `docs/installation-and-usage-v0.1.md`
- Create: `docs/releases/v0.1.0-beta.3.md`

**Interfaces:**
- Consumes: Task 1 canonical Skill trees.
- Produces: one coherent Plugin/release identity `0.1.0-beta.3` suitable for GitHub-backed Plugin sync and GitHub Release packaging.

- [x] **Step 1: Advance the native Plugin version while preserving marketplace identity**

Set `plugins/aegis/.codex-plugin/plugin.json` version to `0.1.0-beta.3`. Keep `.agents/plugins/marketplace.json` unchanged because its schema declares marketplace/plugin source and policy, not release version.

- [x] **Step 2: Generate/bind the beta.3 release manifest**

Create `skillset/releases/aegis-0.1.0-beta.3.json` from the same accepted exact-nine Skill trees and require `python3 scripts/build_aegis_distributions.py --version 0.1.0-beta.3 --check` to report `AEGIS_DISTRIBUTION_STATE_OK` in hosted CI.

- [x] **Step 3: Advance candidate CI and materialization tests**

Hosted RED showed the candidate workflow and current Plugin-materialization test were still explicitly bound to beta.2. Advance only those current-candidate bindings to beta.3. Historical PD-P34 beta.2 evidence/tests remain historical.

- [x] **Step 4: Update user-facing release references**

Update current-version installation references in `README.md` and `docs/installation-and-usage-v0.1.md` to beta.3. Do not invent a beta.3 Installation Kit checksum before publication; use the release-provided `SHA256SUMS`/release manifest.

- [x] **Step 5: Add beta.3 release notes**

Document that beta.3 is a maintenance prerelease carrying PR #45 semantics, advances Plugin release identity behind the stable marketplace source, refreshes the nine-Skill Installation Kit, preserves beta.2 immutability, and excludes unreleased Control Plane v0.2 implementation work.

### Task 3: Hosted verification and maintenance release readiness

**Interfaces:**
- Consumes: exact maintenance PR head.
- Produces: reviewer-accessible CI evidence and a P24 governance decision for merge/release readiness.

- [x] **Step 1: Open PR #46 to `main`**

Title: `release: Aegis v0.1.0-beta.3 maintenance update`.

- [x] **Step 2: Require exact-head hosted checks**

Require Aegis Skillset Integrity and Project State Integrity to pass. Validate generated distributions, beta.3 candidate release manifest, OpenAI Plugin materialization, routing, installed-platform checks, Skillset regressions, Project State regressions, and the protected evaluation corpus.

- [x] **Step 3: Independently inspect the candidate Installation Kit**

Require the downloaded CI artifact to bind to the exact PR head, contain one deterministic beta.3 Installation Kit with exactly nine nested Skill ZIPs plus `release.json`, and require every nested ZIP SHA-256 to equal its release-manifest entry.

- [ ] **Step 4: Run P24 Release Readiness**

Fail closed on semantic drift, mixed release identity, regression failure, unexpected v0.2 scope, historical beta.2 mutation, or missing rollback/recovery boundary. A real ChatGPT `Sync now` observation is intentionally post-publication validation and is not evidence that can exist before the beta.3 source reaches the maintained branch.

### Task 4: Merge, publish, and verify immutable beta.3

**Interfaces:**
- Consumes: P24-accepted exact maintenance PR head.
- Produces: merged `main`, immutable tag `v0.1.0-beta.3`, GitHub prerelease, refreshed Plugin source state, and verified nine-Skill Installation Kit assets.

- [ ] **Step 1: Merge fail-closed against the P24-accepted head**

Use the exact reviewed head SHA when merging PR #46.

- [ ] **Step 2: Verify post-merge `main` and CI**

Resolve the merge result from `main` and require post-merge integrity checks to remain green before tagging.

- [ ] **Step 3: Create immutable tag `v0.1.0-beta.3`**

Tag the exact accepted release commit. Do not move or replace existing release tags.

- [ ] **Step 4: Let `.github/workflows/release.yml` publish**

Require the release workflow to validate tag/manifest/plugin version coherence, build the deterministic nine-Skill Installation Kit, publish `aegis-skill-installation-kit-v0.1.0-beta.3.zip`, publish the standalone release manifest and `SHA256SUMS`, and verify the uploaded asset digest.

- [ ] **Step 5: Verify the published release independently**

Confirm release target equals tag target, version identities equal `0.1.0-beta.3`, required assets exist, and published digests match the workflow evidence.

- [ ] **Step 6: Preserve a forward-integration obligation for v0.2**

Record that the beta.3 maintenance delta must be reconciled into the v0.2 integration baseline after CP-I09 evidence closure; do not mutate the active CP-I09 branch solely to consume beta.3.
