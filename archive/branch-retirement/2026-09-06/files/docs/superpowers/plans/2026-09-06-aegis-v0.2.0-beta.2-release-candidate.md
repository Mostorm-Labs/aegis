# Aegis v0.2.0-beta.2 — Release Candidate Implementation Plan

Status: **P30 / CONTROL_REASONING / MATERIALIZED**

Task family: `RC-I02`

Primary owner: `aegis-implementation`

Release target:

```yaml
release_version: 0.2.0-beta.2
release_tag: v0.2.0-beta.2
release_channel: prerelease
product_form: PLUGIN_PLUS_EXACT_NINE_SKILLS
implementation_scope: RELEASE_PACKAGE_ONLY
```

## 1. Trusted repository baseline

```yaml
repository:
  provider: github
  full_name: Mostorm-Labs/aegis

current_main: c0ec6cccc6675eca98aa3a453a5ad1c6b672d7dc
current_main_tree: 702a8a9629ca51579b4a7ebec413f22d9992c930

published_predecessor:
  version: 0.2.0-beta.1
  tag: v0.2.0-beta.1
  source_revision: 3253abced7a17d66d8754fa84d7953408aae49d4

beta2_identity_preflight:
  tag_exists: false
  github_release_exists: false
  release_branch_exists: false
```

The current native Plugin manifest on `main` still declares `0.2.0-beta.1`; therefore beta.2 requires a bounded release-candidate materialization step rather than direct tag publication.

## 2. Accepted capability delta since beta.1

This release packages repository state already accepted and integrated after beta.1. It does not reopen those Authorities or reimplement them.

### Verification Productization v0.2

The repository now contains the accepted Verification Productization replacement contract and implementation lineage, including the evidence-contract churn that distinguishes durable/reviewer-resolvable evidence from unsupported execution claims.

Current release packaging must preserve the accepted semantics that implementation/CI/provider success is evidence, not self-issued Gate/Authority truth.

### Project State v0.6 capability

Project State v0.6 capability is integrated on `main` through PR #83.

Accepted exact implementation candidate:

`8ffb087e2557fa76cf6c7f7ffdb9effbe63ef333`

Repository merge:

`c0ec6cccc6675eca98aa3a453a5ad1c6b672d7dc`

The merge tree equals the P34-reviewed candidate tree:

`702a8a9629ca51579b4a7ebec413f22d9992c930`

The capability includes v0.6 Gate Decision Binding semantics (`Bound` / affirmative historical `Absent`), deterministic v0.5 -> v0.6 migration support, immutable integrated history, append-only corroborating evidence, and reviewer-resolvable v0.6 proof/reference material.

### Explicit repository-self-state boundary

The Aegis repository root `.aegis/**` intentionally remains schema v0.5 at this release-planning baseline.

That means beta.2 may claim that **Project State v0.6 capability/tooling/Skill contract is shipped**, but it must not claim that the Aegis repository's own root Project State has already been migrated to v0.6 or that real `int-pr82` persistence has already occurred.

This known successor persistence concern is not part of RC-I02 and must not be silently folded into release packaging.

## 3. Execution granularity

RC-I02 uses one capability-level implementation package and one primary Gate review.

```yaml
release_granularity:
  substantive_package_budget: 1
  primary_gate_budget: 1
  split_rule: acceptance_boundary_not_implementation_steps
```

The following steps are implementation details inside one package, not separate lifecycle packages or separate primary Gates.

## 4. RC-I02 — beta.2 release-candidate materialization

Purpose:

> Materialize one exact, reviewer-accessible `v0.2.0-beta.2` prerelease candidate from current accepted `main`, without changing product semantics or publishing the release.

### RC-I02-A — Release identity and RED oracle

Before authored release changes:

1. establish repository identity `Mostorm-Labs/aegis`;
2. establish `task_anchor` ancestry from current main;
3. confirm no `v0.2.0-beta.2` tag;
4. confirm no GitHub Release for `v0.2.0-beta.2`;
5. confirm no existing `release/v0.2.0-beta.2` branch that would create an ambiguous release identity;
6. inspect dirty work and preserve unrelated work;
7. update the existing release-candidate oracle to expect beta.2, then observe RED before creating beta.2 release surfaces.

Collision at any release identity boundary returns `BLOCKED_RELEASE_IDENTITY_COLLISION`; do not silently choose another version.

### RC-I02-B — Deterministic beta.2 release manifest

Generate, do not hand-author Skill tree or ZIP digests:

```bash
python3 scripts/build_aegis_distributions.py \
  --version 0.2.0-beta.2 \
  --write-manifest
```

Required artifact:

`skillset/releases/aegis-0.2.0-beta.2.json`

The manifest must bind the current canonical exact-nine Skill trees on the candidate and must preserve all historical release manifests unchanged.

### RC-I02-C — Native Plugin materialization

Materialize the current exact-nine Plugin at beta.2:

```bash
python3 scripts/build_openai_plugin_materialization.py \
  --release-version 0.2.0-beta.2 \
  --write
```

Required state:

- `plugins/aegis/.codex-plugin/plugin.json.version = 0.2.0-beta.2`;
- Plugin exposes exactly the nine canonical Aegis Skills;
- Plugin Skill content is deterministic generated output from current canonical Skills;
- repository-identity, Verification Productization, Project State v0.6, Gate-review, implementation, and handoff semantics are inherited from current canonical source;
- no tenth Skill, service runtime, or alternate lifecycle owner is introduced.

### RC-I02-D — Release notes and installation pointers

Create:

`docs/releases/v0.2.0-beta.2.md`

Update:

- `docs/installation-and-usage-v0.2.md`
- `README.md`

The beta.2 release notes should describe the user-relevant delta from beta.1, principally:

1. Verification Productization evidence-contract hardening/current Authority convergence;
2. Project State v0.6 capability and Bound/Absent historical binding model;
3. deterministic v0.5 -> v0.6 migration capability;
4. stronger immutable-history / append-only corroborating-evidence enforcement;
5. maintained repository-identity fail-closed execution semantics and exact-nine Plugin distribution.

The notes must also state the deliberate boundary that repository root `.aegis` remains v0.5 and real PR #82 persistence/reconciliation is not claimed by this release candidate.

Do not state beta.2 is already published.

The immediate immutable rollback/reproducibility boundary for beta.2 is published `v0.2.0-beta.1`.

### RC-I02-E — Candidate CI binding

Update only the current release-candidate binding in `.github/workflows/skillset.yml` as needed so exact-head hosted evidence verifies beta.2.

Preserve immutable published-history proof for:

- `v0.2.0-beta.1 @ 3253abced7a17d66d8754fa84d7953408aae49d4`;
- `v0.1.0-beta.3 @ 38bf619ede0615431c7517bc0e07984136af28cf` where that older compatibility proof remains part of the workflow.

The current candidate checkout must verify:

```yaml
release_manifest_check:
  python3 scripts/build_aegis_distributions.py --version 0.2.0-beta.2 --check

plugin_check:
  python3 scripts/build_openai_plugin_materialization.py --release-version 0.2.0-beta.2 --check

candidate_kit_artifact:
  aegis-skill-installation-kit-0.2.0-beta.2-candidate

candidate_archive:
  aegis-skill-installation-kit-0.2.0-beta.2.zip
```

Do not reinterpret the current beta.2 Plugin as the historical beta.1 or beta.3 payload.

Historical release validation must resolve the immutable historical source/tag instead.

### RC-I02-F — Full qualification

The implementation package must require the exact candidate to pass at least:

```bash
python3 -m tools.aegis_skillset.cli validate .
python3 scripts/build_skillset.py --check
python3 -m tools.aegis_skillset.cli distribution-check .
python3 scripts/build_aegis_distributions.py --check
python3 scripts/build_aegis_distributions.py --version 0.2.0-beta.2 --check
python3 scripts/build_openai_plugin_materialization.py --release-version 0.2.0-beta.2 --check
python3 -m tools.aegis_skillset.cli routing-check .
python3 -m tools.aegis_skillset.cli installed-platform-check .
python3 scripts/validate_generated_skills.py

python3 -m unittest discover -s tests/skillset -v
python3 -m unittest discover -s tests/project_state -v
python3 -m unittest discover -s tests/control_plane -v
python3 evals/scripts/validate_corpus.py
python3 -m unittest discover -s evals/tests -v

python3 -m tools.aegis_state.cli validate .
python3 -m tools.aegis_state.cli check .
```

The exact result must be pushed to a reviewer-accessible PR and obtain hosted evidence bound to the exact candidate revision. At minimum require applicable Skillset, Project State, Verification Productization, and any release-candidate-specific checks triggered by the changed paths.

### RC-I02-G — Candidate artifact verification

Build the beta.2 Installation Kit deterministically and independently inspect:

- outer archive identity;
- embedded release manifest identity/version;
- exact-nine nested ZIP inventory;
- nested ZIP SHA-256 values against the release manifest;
- no mixed beta.1/beta.2 current Plugin payload;
- current candidate Plugin parity with canonical Skill content;
- published beta.1 history unchanged.

## 5. Authorized authored scope

P31 may authorize the following release-package paths/patterns:

```text
tests/skillset/test_control_plane_v02_release_candidate.py
tests/skillset/test_openai_plugin_materialization.py   # only if current-version oracle needs the beta.2 identity
tests/skillset/test_workflow_paths.py                  # only if workflow artifact/version oracle needs beta.2

skillset/releases/aegis-0.2.0-beta.2.json

docs/releases/v0.2.0-beta.2.md
docs/installation-and-usage-v0.2.md
README.md

plugins/aegis/.codex-plugin/plugin.json
plugins/aegis/skills/**                                # generated only
.agents/plugins/marketplace.json                       # generated only when deterministic materialization changes it

.github/workflows/skillset.yml
```

Read/reuse without semantic modification:

```text
scripts/build_aegis_distributions.py
scripts/build_openai_plugin_materialization.py
scripts/build_candidate_plugin_parity.py
scripts/build_skillset.py
scripts/validate_generated_skills.py

tools/aegis_skillset/**
tools/aegis_state/**

.github/workflows/release.yml
skillset/distribution.json
skills/**
.aegis/**
```

## 6. Explicit non-goals / forbidden authored scope

RC-I02 does **not** authorize:

```text
root .aegis/** migration to v0.6
real int-pr82 persistence
synthetic or retroactive PR82 Gate Decision
new Product/Modeling/Architecture/Verification semantics
canonical Skill semantic edits merely to make release packaging pass
release workflow semantic changes
SERVICE_PROFILE authorization
rollout expansion
zero-user-turn cross-Primary substantive chaining
new daemon/service/agent/runtime
release tag creation
GitHub Release publication
merge to main during P32
rewrite/move/delete of historical release tags, notes, manifests, or assets
```

If the release candidate appears to require a product/semantic/verification change outside this boundary, stop with `BLOCKED_AUTHORITY` or the more specific existing blocker instead of expanding RC-I02.

## 7. Evidence materialization and review path

The single RC implementation package must return:

```yaml
repository:
  provider: github
  full_name: Mostorm-Labs/aegis
package_ref: <exact P31 commit>
task_anchor:
  revision: c0ec6cccc6675eca98aa3a453a5ad1c6b672d7dc
  relation: ancestor
result_revision: <exact candidate sha>
exact_tree: <tree sha>
materialized_ref: <reviewer-accessible PR URL>
release_version: 0.2.0-beta.2
release_tag: v0.2.0-beta.2
status: READY_FOR_P34 | BLOCKED_*
```

P32 may not publish the tag/Release and may not issue its own Gate verdict.

After P32 materialization, the next Primary is `aegis-gate-review -> P34` for one independent release-candidate Gate. If P34 passes, `aegis-governance -> P24 final release readiness` decides whether publication may proceed.

## 8. P30 disposition

```yaml
P30:
  task_id: RC-I02
  release_version: 0.2.0-beta.2
  package_budget: 1
  primary_gate_budget: 1
  task_anchor: c0ec6cccc6675eca98aa3a453a5ad1c6b672d7dc
  status: READY_FOR_P31
```

Do not execute P32 from this plan alone. P31 must materialize the exact package and execution envelope first.
